import Link from "next/link";
import { KnowledgeShell, dateLabel, label } from "@/components/knowledge-shell";
import { getActionItems, getDeadlines } from "@/lib/api/server";

export const metadata = { title: "Upcoming" };
export default async function UpcomingPage() { const [deadlines, actions] = await Promise.all([getDeadlines().catch(() => ({ items: [], total: 0, limit: 50, offset: 0 })), getActionItems().catch(() => ({ items: [], total: 0, limit: 50, offset: 0 }))]); return <KnowledgeShell eyebrow="Obligations" title="Upcoming" description="Reviewed deadlines and explicit document-backed action items—not recommendations or legal interpretation."><section><h2 className="text-sm font-medium text-stone-900">Deadlines</h2><div className="mt-2 divide-y divide-stone-200 border-y border-stone-200">{deadlines.items.map((item) => <Link key={item.id} href={`/documents/${item.source_document_id}`} className="flex justify-between gap-4 py-4 text-sm hover:bg-stone-100/50"><div><p className="font-medium text-stone-800">{item.title}</p><p className="mt-1 text-xs text-stone-400">{label(item.deadline_type)}{item.original_rule ? ` · ${item.original_rule}` : ""}</p></div><time className="shrink-0 text-xs text-stone-500">{dateLabel(item.due_at)}</time></Link>)}{!deadlines.items.length ? <p className="py-8 text-center text-sm text-stone-400">No open deadlines.</p> : null}</div></section><section className="mt-9"><h2 className="text-sm font-medium text-stone-900">Action items</h2><div className="mt-2 divide-y divide-stone-200 border-y border-stone-200">{actions.items.map((item) => <Link key={item.id} href={`/documents/${item.source_document_id}`} className="flex justify-between gap-4 py-4 text-sm hover:bg-stone-100/50"><div><p className="font-medium text-stone-800">{item.title}</p><p className="mt-1 text-xs text-stone-400">{label(item.life_area)} · {label(item.priority)}</p></div><time className="shrink-0 text-xs text-stone-500">{dateLabel(item.due_at)}</time></Link>)}{!actions.items.length ? <p className="py-8 text-center text-sm text-stone-400">No open action items.</p> : null}</div></section></KnowledgeShell>; }
